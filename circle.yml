dependencies:
  override:
    - npm install
    - rm bower.json

test:
  override:
    - rm -rf bower_components && bower install angular#1.2.x angular-animate#1.2.x angular-mocks#1.2.x underscore && npm test
    - rm -rf bower_components && bower install angular#1.3.x angular-animate#1.3.x angular-mocks#1.3.x underscore && npm test
